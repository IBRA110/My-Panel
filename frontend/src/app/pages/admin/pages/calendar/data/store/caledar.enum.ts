export enum CalendarActionsEnum {
  CHANGE_DATE = '[calendar] change date',
  CHANGE_DATE_TO_TODAY = '[calendar] change date to today',
  CHANGE_CALENDAR = '[calendar] change calendar',
  SELECT_DAY = '[calendar] select day',
}
